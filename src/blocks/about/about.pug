include ../../data/about.data.pug

mixin about()
  - const {title, text} = aboutData;

  section.section.about#about
    .container.section__inner
      +title({text: title[pageLang]})
      p.about__text !{text[pageLang]}

